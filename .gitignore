# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase CLI local cache
supabase/.temp

# Private local seed data (real teams / people — do not commit)
supabase/seed.private.sql

# Logical DB dumps / backup archives at repo root (PII — never commit)
/roles.sql
/schema.sql
/data.sql
/backup.sql
/backup.sql.gz
/backup.tar.gz
/*.dump
/*.dump.gz

# Work in progress feature development
docs/feature_requests_1.md
docs/roadmap.md
